#include <stdio.h>

int main() {
    int a = 10;
    int b = 20;

    if (a > b) {
        printf("%d is larger than %d\n", a, b);
    } else {
        printf("%d is larger than %d\n", b, a);
    }

    return 0;
}
