#include <stdio.h>

int main() {
    int i = 1; // Initialize the variable

    while (i <= 100) { // Condition to check
        printf("%d\n", i);
        i++; // Increment the variable
    }
    
    return 0;
}
